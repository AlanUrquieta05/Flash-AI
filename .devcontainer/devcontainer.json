{
  "name": "Flash-AI Development",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "web",
  "workspaceFolder": "/rails",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "rebornix.ruby",
        "kaiwood.endwise",
        "castwide.solargraph",
        "aliariff.vscode-erb-beautify",
        "misogi.ruby-rubocop"
      ]
    }
  },
  
  "postCreateCommand": "chmod +x bin/* && find vendor/bundle -name cucumber -type f -exec chmod +x {} \\; && find vendor/bundle -path \"*/bin/*\" -type f -exec chmod +x {} \\; && if [ -d \".bundle/bin\" ]; then chmod +x .bundle/bin/*; fi"
} 